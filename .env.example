# 🔒 TRADING ANALYTICS PLATFORM - ENVIRONMENT VARIABLES
# ======================================================
# Copy this file to .env and set appropriate values

# Security Configuration
ENCRYPTION_KEY=                 # Base64 encoded encryption key (generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())")
JWT_SECRET=                     # JWT secret key for authentication (32+ characters)
SECRET_KEY=                     # Application secret key (32+ characters)

# Database Configuration  
DATABASE_URL=                   # Database connection string (e.g., postgresql://user:pass@host:port/db)
DATABASE_POOL_SIZE=10           # Database connection pool size

# Logging Configuration
LOG_LEVEL=INFO                  # Logging level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_FILE=logs/pipeline.log      # Main log file path
AUDIT_LOG_FILE=logs/audit.log   # Audit log file path
MAX_LOG_FILE_SIZE_MB=10         # Maximum log file size before rotation
LOG_BACKUP_COUNT=5              # Number of backup log files to keep

# Pipeline Configuration
MAX_PARALLEL_STAGES=3           # Maximum number of parallel pipeline stages
RETRY_ATTEMPTS=2                # Number of retry attempts for failed operations
TIMEOUT_MINUTES=60              # Operation timeout in minutes
CHECKPOINT_INTERVAL=5           # Checkpoint interval in minutes

# Monitoring & Alerting
ENABLE_MONITORING=true          # Enable real-time monitoring
NOTIFICATION_ENDPOINTS=console  # Comma-separated list of notification endpoints

# API Configuration
API_KEY=                        # External API key for data sources
API_RATE_LIMIT=100              # API rate limit per minute

# Security Features
ENABLE_ENCRYPTION=true          # Enable data encryption
ENABLE_AUDIT_LOGGING=true       # Enable audit logging
AUDIT_LOG_RETENTION_DAYS=365    # Audit log retention period
ENABLE_SECURITY_HEADERS=true    # Enable security headers in API responses

# Session Management
SESSION_ID=                     # Current session identifier (auto-generated)
CLIENT_IP=                      # Client IP address for audit logging

# File Processing
MAX_FILE_SIZE_MB=100            # Maximum file size for processing
ALLOWED_FILE_EXTENSIONS=.csv,.xlsx,.xls,.parquet,.json  # Allowed file extensions

# Development/Debug
DEBUG=false                     # Enable debug mode (DO NOT USE IN PRODUCTION)
ENABLE_PROFILING=false          # Enable performance profiling

# Backup Configuration
BACKUP_ENABLED=true             # Enable automated backups
BACKUP_RETENTION_DAYS=30        # Backup retention period
BACKUP_LOCATION=backups/        # Backup storage location

# Email Configuration (if using email notifications)
SMTP_HOST=                      # SMTP server host
SMTP_PORT=587                   # SMTP server port
SMTP_USERNAME=                  # SMTP username
SMTP_PASSWORD=                  # SMTP password
SMTP_USE_TLS=true              # Use TLS for SMTP

# Slack Configuration (if using Slack notifications)
SLACK_WEBHOOK_URL=              # Slack webhook URL for notifications
SLACK_CHANNEL=                  # Slack channel for notifications

# Performance Configuration
WORKER_PROCESSES=4              # Number of worker processes
MEMORY_LIMIT_MB=2048            # Memory limit per process
CPU_LIMIT_PERCENT=80            # CPU usage limit percentage

# Data Sources
BLOOMBERG_API_KEY=              # Bloomberg API key
REFINITIV_API_KEY=              # Refinitiv API key
FRED_API_KEY=                   # Federal Reserve Economic Data API key

# Cloud Storage (if applicable)
AWS_ACCESS_KEY_ID=              # AWS access key
AWS_SECRET_ACCESS_KEY=          # AWS secret key
AWS_DEFAULT_REGION=us-east-1    # AWS region
S3_BUCKET_NAME=                 # S3 bucket for data storage

# Cache Configuration
REDIS_URL=                      # Redis URL for caching
CACHE_TTL_SECONDS=3600          # Cache time-to-live in seconds

# Notes:
# - Keep this file secure and never commit actual values to version control
# - Use strong, unique values for all secret keys
# - Regularly rotate encryption keys and secrets
# - Ensure database credentials have minimal required permissions
# - Enable all security features in production environments